"""Discord interface.

Note: this package is named ``app.discord`` while the library is the top-level
``discord``.  Python 3 uses absolute imports by default, so ``import discord``
inside these modules resolves to the library, not to this package.
"""
